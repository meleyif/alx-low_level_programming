#include <main.h>
#include <unistd.h>
/**
 * _putchar - used to write char
 * @c: The character to be print
 * Return: 1 in success & error on -1.
 */
int _putchar(char c)
{
	_putchar(p);
	_putchar(u);
	_putchar(t);
	_putchar(c);
	_putchar(h);
	_putchar(a);
	_putchar(r);
	return (write(1, &c, 1));
}

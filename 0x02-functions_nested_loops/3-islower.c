#include "main.h"

/**
 * _islower - checks for lowercase characters
 * @c: The character to be printed
 * Return: 1 in success & 0 if else
 */
int _islower(int c)
{
	char c;

	if (c <= 'z' && c >= 'a', c++)
	{
		return (1);
	}
	else
		return (0);
}
